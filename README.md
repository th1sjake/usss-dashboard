# 🎯 USSS - Unified Staff Statistics System

Система учета и статистики для управления персоналом с функциями отчетности, рейтингов и интеграции с Discord.

## 📋 Возможности

- ✅ **Аутентификация и авторизация** (JWT)
- 📊 **Статистика пользователей** (дневная, недельная, общая)
- 📈 **Таблица лидеров** с фильтрацией
- 📝 **Система отчетов** с модерацией
- 🎯 **Управление задачами** и категориями
- 👥 **Управление пользователями** (ранги, отделы)
- 🔔 **Интеграция с Discord** (уведомления, обновление лидерборда)
- ⚙️ **Профиль пользователя** (самостоятельное редактирование)
- 📅 **Исторические данные** (просмотр статистики по неделям)

## 🛠️ Технологии

### Backend
- **NestJS** - фреймворк для Node.js
- **PostgreSQL** - база данных
- **Prisma** - ORM
- **JWT** - аутентификация
- **Discord.js** - интеграция с Discord

### Frontend
- **React** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **TailwindCSS** - стилизация
- **Recharts** - графики
- **React Hook Form** - формы

### DevOps
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация
- **Nginx** - reverse proxy
- **Let's Encrypt** - SSL сертификаты

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd usss

# Запустите через Docker Compose
docker-compose up -d

# Приложение будет доступно:
# Frontend: http://localhost:5173
# Backend: http://localhost:3000
```

### Production развертывание

См. подробные инструкции:
- 📖 **[DEPLOYMENT.md](./DEPLOYMENT.md)** - полная инструкция по развертыванию
- ⚡ **[QUICKSTART.md](./QUICKSTART.md)** - быстрый старт

## 📁 Структура проекта

```
usss/
├── client/                 # Frontend (React + Vite)
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── pages/         # Страницы
│   │   ├── context/       # Context API
│   │   ├── lib/           # Утилиты
│   │   └── types/         # TypeScript типы
│   ├── Dockerfile         # Dev Dockerfile
│   └── Dockerfile.prod    # Production Dockerfile
│
├── server/                # Backend (NestJS)
│   ├── src/
│   │   ├── auth/         # Аутентификация
│   │   ├── users/        # Управление пользователями
│   │   ├── reports/      # Отчеты и статистика
│   │   ├── tasks/        # Задачи
│   │   ├── discord/      # Discord интеграция
│   │   └── prisma/       # Prisma сервис
│   ├── prisma/
│   │   └── schema.prisma # Схема БД
│   ├── Dockerfile        # Dev Dockerfile
│   └── Dockerfile.prod   # Production Dockerfile
│
├── nginx/                # Nginx конфигурация
│   ├── nginx.conf
│   └── conf.d/
│       └── default.conf
│
├── docker-compose.yml         # Dev окружение
├── docker-compose.prod.yml    # Production окружение
├── deploy.sh                  # Скрипт развертывания
└── .env.production.example    # Пример переменных окружения
```

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env.production` на основе `.env.production.example`:

```env
# Database
POSTGRES_USER=usss_user
POSTGRES_PASSWORD=your_strong_password
POSTGRES_DB=usss

# Backend
JWT_SECRET=your_jwt_secret_min_32_chars
NODE_ENV=production

# Frontend
VITE_API_URL=/api

# Domain
DOMAIN=your-domain.com
```

## 📊 База данных

### Схема

- **User** - пользователи
- **Rank** - ранги
- **Department** - отделы
- **TaskType** - типы задач
- **Report** - отчеты
- **DiscordConfig** - конфигурация Discord

### Миграции

```bash
# Применить миграции
docker exec usss-server npx prisma db push

# Сгенерировать Prisma Client
docker exec usss-server npx prisma generate
```

## 🔐 Безопасность

- JWT токены для аутентификации
- Bcrypt для хеширования паролей
- HTTPS через Let's Encrypt
- CORS настроен
- Валидация входных данных
- Защита от SQL-инъекций (Prisma ORM)

## 📝 API Endpoints

### Аутентификация
- `POST /auth/login` - вход
- `POST /auth/register` - регистрация
- `GET /auth/profile` - профиль

### Пользователи
- `GET /users` - список пользователей (admin)
- `GET /users/me` - текущий пользователь
- `GET /users/ranks` - список рангов
- `PATCH /users/:id` - обновление пользователя

### Отчеты
- `GET /reports` - список отчетов
- `POST /reports` - создать отчет
- `GET /reports/stats` - статистика
- `GET /reports/leaderboard` - таблица лидеров
- `PATCH /reports/:id/status` - изменить статус (admin)

### Задачи
- `GET /tasks` - список задач
- `POST /tasks` - создать задачу (admin)
- `PATCH /tasks/:id` - обновить задачу (admin)
- `DELETE /tasks/:id` - удалить задачу (admin)

### Discord
- `GET /discord/config` - конфигурация
- `POST /discord/config` - обновить конфигурацию (admin)
- `POST /discord/update-leaderboard` - обновить лидерборд

## 🤝 Разработка

### Локальная разработка

```bash
# Запустить dev окружение
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановить
docker-compose down
```

### Обновление зависимостей

```bash
# Backend
cd server
npm update

# Frontend
cd client
npm update
```

## 📦 Деплой

### Автоматический деплой

```bash
# На VPS
cd /var/www/usss
./deploy.sh setup
```

### Ручной деплой

См. [DEPLOYMENT.md](./DEPLOYMENT.md)

## 🐛 Решение проблем

### Проверка логов

```bash
# Все сервисы
docker-compose -f docker-compose.prod.yml logs -f

# Конкретный сервис
docker-compose -f docker-compose.prod.yml logs -f server
```

### Перезапуск

```bash
docker-compose -f docker-compose.prod.yml restart
```

### Бэкап БД

```bash
docker exec usss-postgres pg_dump -U usss_user usss > backup_$(date +%Y%m%d).sql
```

## 👥 Разработка

discord/telegram: dirtyjake
special for USSS Majestic RP #12

---

**Версия**: 1.0.0  
**Последнее обновление**: 2026-02-07
